% Generated by roxygen2: do not edit by hand
\name{marcxml_to_parquet}
\alias{marcxml_to_parquet}
\title{Convert a MARCXML collection to a Parquet dataset}
\usage{
marcxml_to_parquet(
  file,
  output_dir,
  batch_records = 5000L,
  workers = 1L,
  chunk_records = NULL,
  compression = "snappy",
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to a MARCXML collection.}

\item{output_dir}{Path for the new Parquet dataset directory. It must not
already exist. The directory is published only after successful conversion.}

\item{batch_records}{Maximum number of serialized records retained in a batch
before parsing and writing. This bounds normal working memory, though an
unusually large individual record can itself require substantial memory.}

\item{workers}{Number of local worker processes. The default, \code{1}, is
sequential. Values greater than one require the optional parallel packages
listed in \code{Suggests} and, with current dependency versions, R 4.3 or
later.}

\item{chunk_records}{Number of records assigned to each parsing and writing
task. \code{NULL} targets approximately two tasks per worker in each batch.}

\item{compression}{Parquet compression codec passed to
\code{arrow::write_parquet()}.}

\item{verbose}{Whether to report cumulative records and files after each
completed batch.}
}
\value{
Invisibly, a one-row tibble containing normalized input and output paths,
record and row counts, number of batches, and number of Parquet files. Parsed
rows remain in the dataset directory.
}
\description{
\code{marcxml_to_parquet()} streams complete MARCXML records from a collection,
parses them in bounded batches, and writes the canonical long representation
as a directory of Parquet files. It does not construct a DOM for the complete
XML document and does not materialize the complete parsed result in R.
}
\details{
The input must have a \code{collection} root in the official MARCXML namespace
(\code{http://www.loc.gov/MARC21/slim}) or no namespace. A standalone record
can be read with \code{read_marcxml()} but is not accepted by this converter.

Complete records are serialized in the main process before parallel work. This
prevents XML external pointers from crossing process boundaries. With multiple
workers, record strings are exposed through \code{mori} shared memory, and
\code{futurize} dispatches \code{purrr} tasks through a temporary
\code{future.mirai} plan. The previous future plan is restored on exit.

Each task writes a uniquely named temporary file and renames it only after a
successful Parquet write. All files are first written under a staging directory
beside \code{output_dir}; the completed directory is renamed into place only
after the XML input has been fully processed. Existing output is never
overwritten.

Open the result with \code{arrow::open_dataset(output_dir)}. Opening a dataset
is lazy; calling \code{collect()} on the entire dataset will nevertheless
materialize every row in R memory.
}
\examples{
if (requireNamespace("XML", quietly = TRUE) &&
    requireNamespace("arrow", quietly = TRUE)) {
  example_file <- system.file(
    "extdata", "example-marcxml.xml", package = "marcxmlr"
  )
  output <- tempfile("marcxml-parquet-")

  conversion <- marcxml_to_parquet(
    example_file,
    output_dir = output,
    workers = 1L,
    verbose = FALSE
  )

  dataset <- arrow::open_dataset(output)
  conversion
  dataset

  unlink(output, recursive = TRUE)
}
}
