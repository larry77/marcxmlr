% Generated by roxygen2: do not edit by hand
\name{read_marcxml}
\alias{read_marcxml}
\title{Read MARCXML into a canonical long tibble}
\usage{
read_marcxml(file, n_max = Inf, workers = 1L, chunk_records = NULL)
}
\arguments{
\item{file}{Path to a MARCXML file.}

\item{n_max}{Maximum number of records to parse. Use \code{Inf} for every
record or \code{0} to return an empty result with the canonical schema.}

\item{workers}{Number of local worker processes. The default, \code{1}, is
sequential. Values greater than one require the optional parallel packages
listed in \code{Suggests}.}

\item{chunk_records}{Number of records assigned to each parsing task.
\code{NULL} creates one task in sequential mode and approximately two tasks
per worker in parallel mode.}
}
\value{
A tibble with columns \code{record_id}, \code{field_type}, \code{tag},
\code{subfield_code}, \code{value}, \code{field_order},
\code{field_occurrence}, \code{ind1}, \code{ind2}, \code{subfield_order}, and
\code{subfield_occurrence}, in that order.
}
\description{
\code{read_marcxml()} reads a MARC21 XML collection or a standalone record and
returns one row for each leader, control field, or data-field subfield. It
preserves repeated fields, repeated subfields, indicators, and source order.
}
\details{
This function materializes both the XML input and parsed result in memory. Use
\code{marcxml_to_parquet()} for catalogues that may not fit in memory.

\code{record_id} is positional identity in the selected input and is not
derived from control field \code{001}. \code{field_order} is zero for the
leader and then counts variable fields from one. \code{field_occurrence}
counts occurrences of a field type and tag within a record.

Data-field rows carry \code{subfield_order}, the position within the containing
field, and \code{subfield_occurrence}, the occurrence of that code within the
same field. Structural columns that do not apply are \code{NA}.

Parallel parsing serializes complete records before dispatch, so \code{xml2}
external pointers are never sent to workers. The caller's previous future plan
is restored on exit.
}
\examples{
example_file <- system.file(
  "extdata", "example-marcxml.xml", package = "marcxmlr"
)

records <- read_marcxml(example_file)
records

records[
  records$record_id == 1L & records$tag == "856",
  c("subfield_code", "value", "subfield_order", "subfield_occurrence")
]
}
