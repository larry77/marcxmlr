% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-marcxml.R
\name{write_marcxml}
\alias{write_marcxml}
\title{Write a canonical MARC representation as MARCXML}
\usage{
write_marcxml(
  x,
  file,
  check = TRUE,
  pretty = TRUE,
  records_per_file = Inf,
  compression_level = 6L
)
}
\arguments{
\item{x}{A data frame or tibble containing the canonical 11-column
\code{marcxmlr} representation, or an Arrow \code{Dataset} / lazy
\code{arrow_dplyr_query} with the same columns. Arrow inputs require the
optional \code{arrow} package and must present complete record groups in
non-decreasing \code{record_id} order.}

\item{file}{Output XML path. When \code{records_per_file} is finite, this path is
used as the stem for deterministic shard names such as
\verb{catalogue-00001.xml}. Existing target files are not overwritten. Output
files are staged before being committed so a serialization failure does
not leave a partial shard family.}

\item{check}{Whether to emit warning-level canonical diagnostics before
writing. Structurally ambiguous input is always rejected, including when
\code{check = FALSE}.}

\item{pretty}{Whether to format the output XML with indentation.}

\item{records_per_file}{Maximum number of complete MARC records per output
file. The default, \code{Inf}, writes one collection. A finite value writes
numbered collection shards and never splits a record.}

\item{compression_level}{Gzip compression level from 1 to 9. The default is
\code{6}. It is used only when \code{file} ends in \code{.gz}; other filenames are
written as uncompressed XML.}
}
\value{
Invisibly, a character vector containing the output path or paths.
}
\description{
Serialize a canonical \code{marcxmlr} representation to MARCXML. In-memory
data frames use the direct in-memory path; Arrow Datasets and lazy Arrow
queries are consumed incrementally in bounded batches.
MARC record structure and values are preserved, but incidental XML
serialization details such as indentation, namespace-prefix spelling,
comments, entity spelling, and the original XML declaration are not.
}
\details{
Records are written in ascending \code{record_id} order. Within each record,
\code{field_order} and \code{subfield_order} determine field and subfield ordering.
The occurrence columns are diagnostics only and are not used to construct
the XML.

Use \code{\link[=diagnose_canonical]{diagnose_canonical()}} to inspect structural errors and warning-level
coordinate issues without writing an XML file.
}
\examples{
example_file <- system.file(
  "extdata", "example-marcxml.xml", package = "marcxmlr"
)
x <- read_marcxml(example_file)
out <- tempfile(fileext = ".xml")
write_marcxml(x, out)

shard_dir <- tempfile("marcxmlr-shards-")
dir.create(shard_dir)
shard_stem <- file.path(shard_dir, "catalogue.xml")
write_marcxml(x, shard_stem, records_per_file = 1)
unlink(shard_dir, recursive = TRUE)
}
\seealso{
\code{\link[=diagnose_canonical]{diagnose_canonical()}}
}
